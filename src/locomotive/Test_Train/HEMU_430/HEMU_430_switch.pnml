/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_TRAINS, SELF, sw_HEMU_430_engine, position_in_consist_from_end == 0 && num_vehs_in_consist > 1){
    1: set_HEMU_430_engineT;
    set_HEMU_430_engineH;
}

switch(FEAT_TRAINS, SELF, sw_HEMU_430_1F_wagon, position_in_consist) {
    1: set_HEMU_430_1F_wagon_r;
    set_HEMU_430_1F_wagon;
}

switch(FEAT_TRAINS, SELF, sw_HEMU_430_wagon_attach, vehicle_type_id) {
    HEMU_430:               return CB_RESULT_ATTACH_ALLOW;
    YST_1F_Passenger_Car:   return CB_RESULT_ATTACH_ALLOW;
    YST_2F_Passenger_Car:   return CB_RESULT_ATTACH_ALLOW;
    return string(STR_ERROR_NO_ATTACH);
}